//Crie uma arrow function chamada isAdult que recebe uma idade e retorna true se a idade for 18 ou maior, e false caso contrário.

const isAdult = (age) => age >= 18;

const x = 10;
console.log(isAdult(x))

